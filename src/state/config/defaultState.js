let DEFAULT_STATE = {};

DEFAULT_STATE.FILTER = "All";

DEFAULT_STATE.FILTERS = ["All"];

DEFAULT_STATE.PAGE = 0;

DEFAULT_STATE.NUMBER_OF_PAGES = 4 ;

DEFAULT_STATE.NUMBER_OF_RESULT = 0;

// a set of arrays of movies with integer key as page index 
DEFAULT_STATE.MOVIES = {};

export default DEFAULT_STATE; 